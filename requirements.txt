# JARVIS — Python dependencies
# Install with: pip install -r requirements.txt
#
# Phase 0: setup  (venv, git, .gitignore, .env)  [DONE]
# Phase 1: voice loop (mic + STT + TTS)          [DONE]
# Phase 2: wake word ("Hey Jarvis") + LLM brain  [DONE]
# Phase 3: "open any app" skill                  [DONE]  (uses only stdlib:
#           os.startfile + subprocess, NO new deps)
# Phase 3 (continued): WhatsApp + Gmail email   [DONE] (deep-link + SMTP)
# Phase 4: web search (duckduckgo-search)        [DONE]
# Phase 6: system tray (pystray)                 [DONE]

# --- Audio capture (Phase 1) ---
sounddevice==0.5.5
numpy>=1.26.0,<2

# --- Speech-to-text (Phase 1) ---
# faster-whisper pulls in ctranslate2, onnxruntime, huggingface-hub, etc.
faster-whisper==1.2.1

# --- Text-to-speech (Phase 1) ---
piper-tts==1.4.2

# --- Wake word (Phase 2) ---
# openWakeWord ships a prebuilt "hey jarvis" model. It runs locally on CPU
# via onnxruntime (already pulled in by faster-whisper). Download the model
# files once with:  python -m jarvis.wakeword --download
openwakeword==0.6.0

# --- LLM brain config (Phase 2) ---
# Lets jarvis/brain.py read OPENROUTER_API_KEY from a local .env file.
# The brain itself uses only the standard library (urllib) to call the API.
python-dotenv==1.2.2

# --- Web search skill (Phase 4) ---
# Uses the local DDG server protocol (no browser). Async methods require an
# event loop; we use the sync `text()` method which blocks the main thread
# briefly.
duckduckgo-search>=6.0

# --- System tray (Phase 6) ---
pystray==0.19.5
pillow>=10.0.0